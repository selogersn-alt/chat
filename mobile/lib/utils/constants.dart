import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF0F4F2C);
  static const Color secondary = Color(0xFF1B6B3E);
  static const Color background = Color(0xFFECE5DD); // WhatsApp background
  static const Color surface = Colors.white;
  static const Color bubbleSelf = Color(0xFFE7FFDB); // WhatsApp light green
  static const Color bubbleOther = Colors.white;
}

class AppConstants {
  static const String defaultServerUrl = 'https://chat.logersenegal.com';
  // If testing locally on Android emulator, use: 'http://10.0.2.2:8000'
}
